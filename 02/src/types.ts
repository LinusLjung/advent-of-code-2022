export type InputPlayer = 'X' | 'Y' | 'Z';

export type InputOpponent = 'A' | 'B' | 'C';

export enum Result {
  Win,
  Loss,
  Draw,
}

export enum Move {
  Rock,
  Paper,
  Scissors,
}

export type InputResult = 'X' | 'Y' | 'Z';
